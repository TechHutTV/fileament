services:
  fileament:
    build: .
    ports:
      - "8080:8080"
    environment:
      FILEAMENT_DATA_DIR: /data
      FILEAMENT_PORT: "8080"
    volumes:
      - fileament-data:/data

volumes:
  fileament-data:
